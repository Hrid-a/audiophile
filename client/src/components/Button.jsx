
const Button = (props) => {
    return (
        <>
            <button className={`${props.class} py-4 px-8 text-white text-sm font-medium uppercase tracking-widest `}>
                see product
            </button>
        </>
    )
}

export default Button